import { StyleSheet } from "react-native";

export default contactStyle = StyleSheet.create({
    container: {
       flex: 1,
       justifyContent: 'center',
       marginTop: 10
    },
    avatar: {
      backgroundColor: "gray"
    },
    emptyMessage: {
      textAlign: "center"
    }
});